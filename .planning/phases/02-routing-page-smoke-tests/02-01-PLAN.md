---
phase: 02-routing-page-smoke-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - src/App.test.jsx
  - src/pages/Layout.test.jsx
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04

must_haves:
  truths:
    - "D-01: src/App.jsx exports a named `function AppRoutes()` containing `<Layout><Routes>...</Routes></Layout>`; the default `App` export remains `<HashRouter><AppRoutes/></HashRouter>` so production runtime behavior of App is unchanged"
    - "D-02: src/App.test.jsx fully replaces the Phase 1 banner-only smoke test with a route-table test that mounts `<MemoryRouter initialEntries={[path]}><AppRoutes/></MemoryRouter>` once per route (5 paths + /bogus); the Phase 1 banner-only test is dropped, not kept alongside"
    - "D-03: src/pages/Layout.test.jsx renders `<MemoryRouter><Layout>{stubChild}</Layout></MemoryRouter>`, asserts `getByRole('banner')`, asserts the 4 nav Link hrefs (`/`, `/resume`, `/portfolio`, `/contact` — About intentionally points to `/` per Layout.jsx:30), and asserts the stub child renders"
    - "D-11: Layout.test.jsx uses the ARIA `getByRole('banner')` pattern where it reads cleanly; per-page sentinels in App.test.jsx use the D-09 section-id contract"
    - "D-12: No `<Route path=\"*\">` catch-all or redirect is added to src/App.jsx; ROUTE-04 is tested against current behavior — `/bogus` renders Layout chrome but no page section id"
    - "D-13: Plan 02-01 owns src/App.jsx, src/App.test.jsx, and src/pages/Layout.test.jsx (disjoint from Plan 02-02's page-test files)"
    - "Rendering AppRoutes under MemoryRouter at '/' shows the Home section (#hero)"
    - "Rendering AppRoutes under MemoryRouter at '/about', '/resume', '/portfolio', '/contact' shows the matching section id"
    - "Rendering AppRoutes under MemoryRouter at '/bogus' does not throw and shows Layout chrome but no page section id"
    - "Layout renders a banner landmark and passes its children through to the DOM"
    - "Layout's nav exposes 4 links with hrefs '/', '/resume', '/portfolio', '/contact' (the 'About' label intentionally points to '/' per Layout.jsx:30)"
    - "App.test.jsx, Layout.test.jsx, and the section-id contract are the single source of truth shared with Plan 02-02"
  artifacts:
    - path: "src/App.jsx"
      provides: "Default App (HashRouter wrapper) AND named export `AppRoutes` returning <Layout><Routes>...</Routes></Layout>"
      contains: "export function AppRoutes"
    - path: "src/App.test.jsx"
      provides: "Route-table tests under MemoryRouter covering '/', '/about', '/resume', '/portfolio', '/contact', and '/bogus'"
      contains: "MemoryRouter"
    - path: "src/pages/Layout.test.jsx"
      provides: "Isolated Layout chrome test (banner role, 4 nav link hrefs, children pass-through)"
      contains: "getByRole('banner')"
  key_links:
    - from: "src/App.test.jsx"
      to: "src/App.jsx (AppRoutes named export)"
      via: "import { AppRoutes } from './App'"
      pattern: "import\\s+App,\\s*\\{\\s*AppRoutes\\s*\\}\\s+from\\s+'\\./App'"
    - from: "src/App.test.jsx"
      to: "react-router-dom MemoryRouter"
      via: "<MemoryRouter initialEntries={[path]}><AppRoutes/></MemoryRouter>"
      pattern: "MemoryRouter\\s+initialEntries"
    - from: "src/pages/Layout.test.jsx"
      to: "src/pages/Layout.jsx"
      via: "import Layout from './Layout' rendered inside MemoryRouter with stub child"
      pattern: "import\\s+Layout\\s+from\\s+'\\./Layout'"
---

<objective>
Wire routing under test for every URL the portfolio supports. Refactor `src/App.jsx` so its route table is testable in isolation, replace the Phase 1 banner-only smoke test with a route-table test, and add an isolated `Layout` test for chrome + children pass-through.

Purpose: Lock the routing contract (ROUTE-01..04) and the `Layout` chrome contract (ROUTE-03) so any regression in route wiring or in Layout's header/nav fails the test suite. Also establishes the section-id contract (`#hero`, `#about`, `#resume`, `#portfolio`, `#contact`) that Plan 02-02 will reuse.

Output:
- `src/App.jsx` — adds `export function AppRoutes()` alongside default `App` (no behavior change).
- `src/App.test.jsx` — fully replaces Phase 1 contents with a route-table test (5 routes + 1 unknown route).
- `src/pages/Layout.test.jsx` — new isolated Layout test.
</objective>

<execution_context>
@/Users/hariraj.venkatesan/repos/harirajv.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hariraj.venkatesan/repos/harirajv.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-routing-page-smoke-tests/02-CONTEXT.md
@.planning/phases/01-test-infrastructure/01-01-SUMMARY.md
@CLAUDE.md

# Source files this plan touches or reads
@src/App.jsx
@src/App.test.jsx
@src/pages/Layout.jsx
@src/setupTests.js
@vite.config.mjs

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + CONTEXT.md D-numbers. -->
<!-- No codebase exploration required — these are the contracts. -->

Current src/App.jsx (the only file that must change for runtime code):
```
import React from 'react';
import { HashRouter, Route, Routes } from 'react-router-dom';
import './App.css';
import Layout from './pages/Layout';
import Home from './pages/Home';
import About from './pages/About';
import Resume from './pages/Resume';
import Portfolio from './pages/Portfolio';
import Contact from './pages/Contact';

function App() {
  return (
    <HashRouter>
      <Layout>
        <Routes>
          <Route exact path="/" element={<Home/>} />
          <Route path="/about" element={<About/>} />
          <Route path="/resume" element={<Resume/>} />
          <Route path="/portfolio" element={<Portfolio/>} />
          <Route path="/contact" element={<Contact/>} />
        </Routes>
      </Layout>
    </HashRouter>
  );
}

export default App;
```

After this plan src/App.jsx must look like (D-01):
- A named `export function AppRoutes()` that returns `<Layout><Routes>...the same 5 Route entries...</Routes></Layout>`.
- The default `App` becomes `<HashRouter><AppRoutes/></HashRouter>`.
- All five imports (Layout, Home, About, Resume, Portfolio, Contact) stay.
- Nothing else changes — same routes, same paths, same elements, same exact path "/" on Home.

Section id contract (D-09, shared with Plan 02-02 — single source of truth):
| Path        | Section id     |
|-------------|----------------|
| /           | #hero          |
| /about      | #about         |
| /resume     | #resume        |
| /portfolio  | #portfolio     |
| /contact    | #contact       |

Layout nav contract (Layout.jsx:30 — D-03 locks current behavior):
- Header items are ["About", "Resume", "Portfolio", "Contact"].
- Link hrefs: "About" -> "/", "Resume" -> "/resume", "Portfolio" -> "/portfolio", "Contact" -> "/contact".
- Header tag is `<header id="header">` (semantic `<header>` => ARIA role 'banner' — Phase 1 already used `getByRole('banner')`).

Vitest globals (vite.config.mjs `test.globals: true`): describe, it, expect, vi, beforeEach, afterEach are callable without imports. setupTests.js already loads `@testing-library/jest-dom`. Do NOT modify setupTests.js.

Side-effect risk when rendering AppRoutes at any path:
- Home (`/`) imports `typed.js` and calls `new Typed(...)` in componentDidMount. The App.test.jsx route-table test renders Home at "/" — this WILL invoke typed.js unless mocked at the file scope of App.test.jsx. Use the D-05 mock at the top of App.test.jsx so the "/" case is zero-cost and leak-free.
- Portfolio (`/portfolio`) calls `fetch("assets/projects.json")` in componentDidMount. In jsdom this would attempt a real (failing) fetch. Spy on `global.fetch` at the top of App.test.jsx (D-06 pattern). The portfolio route assertion can wait via `waitFor` for `#portfolio` to appear, OR — since Portfolio's render() outputs the `<section id="portfolio">` synchronously regardless of fetch state — assert `#portfolio` immediately after render. Either is acceptable per D-06; the simpler synchronous assertion is preferred for the routing test (the fetch-mock is still required so the unhandled promise does not warn in test output).
- Contact (`/contact`) imports `@emailjs/browser` but only calls `sendForm` on submit (D-07). No mock needed for render-only.
- About, Resume: synchronous, no side effects (D-08).

Unknown route (D-12):
- No `<Route path="*">` added. react-router-dom 6 will render Layout chrome but nothing in the `<Routes>` slot.
- Test at `/bogus`: assert `getByRole('banner')` exists, and assert NONE of the 5 section ids is present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor src/App.jsx to export AppRoutes (no runtime change)</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (the current contents — to be split, not rewritten)
    - .planning/phases/02-routing-page-smoke-tests/02-CONTEXT.md (decisions D-01, D-02, D-13)
    - src/App.test.jsx (Phase 1 test — its assertion `screen.getByRole('banner')` after `render(<App />)` MUST keep passing after this refactor, until Task 2 replaces the file)
  </read_first>
  <behavior>
    - After change: importing `App` from './App' returns a component that mounts HashRouter > Layout > Routes (5 routes), identical to the pre-change render tree.
    - After change: importing `AppRoutes` from './App' returns a component that renders Layout > Routes (5 routes) — no router.
    - Rendering `<App/>` in jsdom still satisfies the Phase 1 assertion `screen.getByRole('banner')` (proves runtime behavior is unchanged).
    - Rendering `<MemoryRouter initialEntries={['/']}><AppRoutes/></MemoryRouter>` mounts Home (renders `#hero` in the DOM).
  </behavior>
  <action>
    Refactor src/App.jsx per D-01. Keep ALL existing imports (Layout, Home, About, Resume, Portfolio, Contact, react-router-dom HashRouter+Route+Routes, './App.css'). Add a named function export `AppRoutes` that returns the same `<Layout><Routes>...5 Route entries...</Routes></Layout>` JSX currently inside `App`. Update `App` to return `<HashRouter><AppRoutes/></HashRouter>`. Keep `export default App`. Do not change paths, do not change element components, do not change the `exact path="/"` attribute on Home, do not add a catch-all route (D-12 — unknown routes lock to current behavior). Do not touch any other file in this task.
  </action>
  <verify>
    <automated>npm test -- --run 2>&amp;1 | tail -20 &amp;&amp; grep -q "export function AppRoutes" src/App.jsx &amp;&amp; grep -q "export default App" src/App.jsx &amp;&amp; grep -q "HashRouter" src/App.jsx &amp;&amp; grep -q "AppRoutes" src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function AppRoutes" src/App.jsx` exits 0
    - `grep -q "export default App" src/App.jsx` exits 0
    - `grep -q "<HashRouter>" src/App.jsx` exits 0 (HashRouter still in default App)
    - `grep -c "<Route " src/App.jsx` returns exactly 5 (five Route entries preserved)
    - `grep -q 'path="\\*"' src/App.jsx` returns nothing (no catch-all added per D-12)
    - `npm test -- --run` exits 0 (Phase 1 test still passes because App still renders Layout + banner)
    - No other files are modified in this task (`git diff --name-only` shows only `src/App.jsx`)
  </acceptance_criteria>
  <done>src/App.jsx exports both `App` (default, HashRouter-wrapped) and `AppRoutes` (named, router-agnostic). `npm test -- --run` is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite src/App.test.jsx as a route-table test (ROUTE-01..04)</name>
  <files>src/App.test.jsx</files>
  <read_first>
    - src/App.jsx (post-Task-1 — confirms AppRoutes is the right import)
    - src/pages/Home.jsx (confirms Home imports typed.js — drives D-05 mock at top of test file)
    - src/pages/Portfolio.jsx (confirms fetch usage — drives D-06 spy at top of test file)
    - .planning/phases/02-routing-page-smoke-tests/02-CONTEXT.md (decisions D-02, D-05, D-06, D-09, D-12)
    - .planning/phases/01-test-infrastructure/01-01-SUMMARY.md (Vitest globals + jest-dom matcher patterns)
  </read_first>
  <behavior>
    - Test "renders Home at /" asserts `container.querySelector('#hero')` is in the document AND `screen.getByRole('banner')` is in the document.
    - Test "renders About at /about" asserts `container.querySelector('#about')`.
    - Test "renders Resume at /resume" asserts `container.querySelector('#resume')`.
    - Test "renders Portfolio at /portfolio" asserts `container.querySelector('#portfolio')` (synchronous — Portfolio's render emits the section regardless of fetch state, but fetch must still be spied to avoid jsdom network noise).
    - Test "renders Contact at /contact" asserts `container.querySelector('#contact')`.
    - Test "renders Layout chrome but no page section on unknown route" with `initialEntries={['/bogus']}` asserts: `screen.getByRole('banner')` present, AND `container.querySelector('#hero')` is null, AND `container.querySelector('#about')` is null, AND `container.querySelector('#resume')` is null, AND `container.querySelector('#portfolio')` is null, AND `container.querySelector('#contact')` is null.
    - The file-level mocks (`vi.mock('typed.js', ...)` and `vi.spyOn(global, 'fetch', ...)`) do not bleed across tests (use `afterEach(() => vi.restoreAllMocks())` or rely on Vitest restore — either is acceptable per D-04 discretion).
  </behavior>
  <action>
    Fully replace the contents of src/App.test.jsx (D-02 — the Phase 1 banner-only test is dropped, route assertions inherently cover Layout chrome). The new file MUST:

    1. Import from '@testing-library/react': `render`, `screen`. Import `MemoryRouter` from 'react-router-dom'. Import the named export `AppRoutes` from './App'.
    2. Declare `vi.mock('typed.js', () => ({ default: class { constructor() {} destroy() {} } }))` at module scope (D-05) — required because the "/" case mounts Home which calls `new Typed(...)` in componentDidMount.
    3. Inside a single `describe('App routing', ...)`:
       a. A `beforeEach` that does `vi.spyOn(global, 'fetch').mockResolvedValue({ ok: true, json: () => Promise.resolve({ projects: [] }) })` (D-06).
       b. An `afterEach` that does `vi.restoreAllMocks()`.
       c. One `it()` per path in the D-09 mapping (`/`, `/about`, `/resume`, `/portfolio`, `/contact`). Each test does `const { container } = render(<MemoryRouter initialEntries={[path]}><AppRoutes/></MemoryRouter>)`, then asserts `container.querySelector('#<section-id>')` is not null (use `.toBeInTheDocument()`).
       d. One `it('renders Layout chrome but no page section on unknown route', ...)` rendering at `initialEntries={['/bogus']}`, asserting `screen.getByRole('banner')` present AND each of the 5 section ids returns null from `container.querySelector` (use `.not.toBeInTheDocument()` after `expect(container.querySelector(...))` OR plain `expect(container.querySelector('#hero')).toBeNull()`).

    Do NOT add manual `afterEach(cleanup)` — RTL auto-cleans under Vitest with globals. Do NOT import `describe`/`it`/`expect`/`vi` — they are globals (vite.config.mjs `test.globals: true`). Do NOT modify src/setupTests.js (D-04). Do NOT add per-test mocks of `@emailjs/browser` (D-07 — Contact render is side-effect-free).
  </action>
  <verify>
    <automated>npm test -- --run src/App.test.jsx 2>&amp;1 | tail -30 &amp;&amp; grep -c "MemoryRouter initialEntries" src/App.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import.*AppRoutes.*from '\\./App'" src/App.test.jsx` exits 0 (AppRoutes is imported from the local file)
    - `grep -q "MemoryRouter" src/App.test.jsx` exits 0
    - `grep -c "MemoryRouter initialEntries" src/App.test.jsx` returns at least 6 (5 known routes + 1 unknown)
    - `grep -q "vi.mock('typed.js'" src/App.test.jsx` exits 0 (D-05 mock present)
    - `grep -q "vi.spyOn(global, 'fetch'" src/App.test.jsx` exits 0 (D-06 spy present)
    - `grep -q "container.querySelector('#hero')" src/App.test.jsx` exits 0
    - `grep -q "container.querySelector('#about')" src/App.test.jsx` exits 0
    - `grep -q "container.querySelector('#resume')" src/App.test.jsx` exits 0
    - `grep -q "container.querySelector('#portfolio')" src/App.test.jsx` exits 0
    - `grep -q "container.querySelector('#contact')" src/App.test.jsx` exits 0
    - `grep -q "/bogus" src/App.test.jsx` exits 0 (unknown-route case covered)
    - `npm test -- --run src/App.test.jsx` exits 0 with all 6 (or more) tests passing
    - The phrase "learn react" appears 0 times in src/App.test.jsx (CRA boilerplate is gone — defensive check)
  </acceptance_criteria>
  <done>src/App.test.jsx is a route-table test asserting one section id per route plus an unknown-route case that asserts Layout chrome without any page section. `npm test -- --run src/App.test.jsx` is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add isolated src/pages/Layout.test.jsx (ROUTE-03)</name>
  <files>src/pages/Layout.test.jsx</files>
  <read_first>
    - src/pages/Layout.jsx (confirms header_items order, the "About" -> "/" mapping at line 30, and the `<header id="header">` semantic tag → banner role)
    - .planning/phases/02-routing-page-smoke-tests/02-CONTEXT.md (decisions D-03, D-11)
    - .planning/phases/01-test-infrastructure/01-01-SUMMARY.md (patterns-established: ARIA banner role assertion)
  </read_first>
  <behavior>
    - `render(<MemoryRouter><Layout><div data-testid="stub-child">stub</div></Layout></MemoryRouter>)` does not throw.
    - `screen.getByRole('banner')` returns a node (the `<header id="header">` element).
    - The 4 nav links have hrefs in this exact order (matching Layout.jsx header_items + the About-link-points-to-"/" rule on line 30):
      - link text "About" → `/`
      - link text "Resume" → `/resume`
      - link text "Portfolio" → `/portfolio`
      - link text "Contact" → `/contact`
    - The stub child node (`data-testid="stub-child"`) is in the document — children pass-through works.
  </behavior>
  <action>
    Create src/pages/Layout.test.jsx per D-03. The file MUST:

    1. Import from '@testing-library/react': `render`, `screen`. Import `MemoryRouter` from 'react-router-dom'. Import the default export `Layout` from './Layout'.
    2. Inside `describe('Layout', ...)`:
       a. One `it('renders banner header')` that asserts `screen.getByRole('banner')` is in the document.
       b. One `it('renders the 4 nav links with expected hrefs')` that queries each link by its visible label text (use `screen.getByRole('link', { name: /About/i })`, etc., OR `screen.getAllByRole('link')` and assert names+hrefs). Assert href values: About → ends with '/' (note: react-router Link in MemoryRouter renders `<a href="/">` when `to="/"`), Resume → '/resume', Portfolio → '/portfolio', Contact → '/contact'. Use either `toHaveAttribute('href', expected)` or `.getAttribute('href') === expected`.
       c. One `it('passes children through')` that renders `<Layout><div data-testid="stub-child">stub</div></Layout>` inside MemoryRouter and asserts `screen.getByTestId('stub-child')` is in the document.

    Every test wraps `<Layout>` in `<MemoryRouter>` because `<Link>` requires a router ancestor. Use Phase 1 ARIA-role pattern (D-11) where it reads cleanly — banner role here. Do not modify src/pages/Layout.jsx.
  </action>
  <verify>
    <automated>npm test -- --run src/pages/Layout.test.jsx 2>&amp;1 | tail -20 &amp;&amp; grep -q "getByRole('banner')" src/pages/Layout.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/Layout.test.jsx` exists
    - `grep -q "import Layout from './Layout'" src/pages/Layout.test.jsx` exits 0
    - `grep -q "MemoryRouter" src/pages/Layout.test.jsx` exits 0
    - `grep -q "getByRole('banner')" src/pages/Layout.test.jsx` exits 0 (D-11 banner assertion present)
    - `grep -q "stub-child" src/pages/Layout.test.jsx` exits 0 (children pass-through test present)
    - `grep -E "'/resume'|\"/resume\"" src/pages/Layout.test.jsx` matches (Resume href asserted)
    - `grep -E "'/portfolio'|\"/portfolio\"" src/pages/Layout.test.jsx` matches (Portfolio href asserted)
    - `grep -E "'/contact'|\"/contact\"" src/pages/Layout.test.jsx` matches (Contact href asserted)
    - `grep -q "About" src/pages/Layout.test.jsx` exits 0 (About link is exercised — its href is "/" per Layout.jsx:30 — assert current behavior, do not fix)
    - `npm test -- --run src/pages/Layout.test.jsx` exits 0 (all Layout tests pass)
    - `npm test -- --run` (full suite) exits 0 — no regression in App.test.jsx
  </acceptance_criteria>
  <done>src/pages/Layout.test.jsx asserts banner role, the 4 nav link hrefs (including "About" → "/"), and children pass-through. Full Vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase only adds test code and performs a tiny refactor that splits one function into a function + a named export. No new runtime branches, no new input surfaces, no production-shipped code paths added.

| Boundary | Description |
|----------|-------------|
| dev environment / production bundle | Test files (`*.test.jsx`) and jsdom-only mocks live in dev only; they are not bundled by Vite's production build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `src/App.jsx` refactor (default `App` semantics) | mitigate | The refactor is constrained to extract the existing `<Layout><Routes>...</Routes></Layout>` JSX into a named `AppRoutes` export and have default `App` render `<HashRouter><AppRoutes/></HashRouter>`. Verified by `npm test` continuing to pass with Phase 1's existing banner assertion before Task 2 replaces the test. Acceptance criteria pin the route count to exactly 5 (`grep -c "<Route " src/App.jsx == 5`) and forbid a catch-all route (`grep -q 'path="\\*"' src/App.jsx` returns nothing). |
| T-02-02 | Spoofing | Test-only `typed.js` and `fetch` mocks | accept | `vi.mock('typed.js', ...)` and `vi.spyOn(global, 'fetch', ...)` are file-scoped to `src/App.test.jsx`. They do not leak into the production bundle (Vitest mocks are isolated to the test runtime). `afterEach(() => vi.restoreAllMocks())` (or Vitest's `restoreMocks: true` default behavior under globals) prevents inter-test bleed. |
| T-02-03 | Information Disclosure | Layout test asserting nav link hrefs | accept | No PII, no secrets — link hrefs are public route paths already in `Layout.jsx`. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (Vitest, jsdom, @testing-library/* all installed in Phase 1). No package-legitimacy gate triggered. |
</threat_model>

<verification>
- `npm test -- --run` exits 0
- All three files compile and run under Vitest jsdom env
- `src/App.jsx` still exports default `App` (production behavior unchanged)
- `src/App.jsx` exports named `AppRoutes` (testable router-agnostic tree)
- `src/App.test.jsx` covers 5 known routes + 1 unknown route
- `src/pages/Layout.test.jsx` covers banner role + 4 nav hrefs + children pass-through
- No modifications outside this plan's `files_modified`
</verification>

<success_criteria>
- ROUTE-01 satisfied: route `/` renders Home (asserted by `#hero` in src/App.test.jsx)
- ROUTE-02 satisfied: routes `/about`, `/resume`, `/portfolio`, `/contact` render their pages (asserted by `#about`, `#resume`, `#portfolio`, `#contact` in src/App.test.jsx)
- ROUTE-03 satisfied: Layout chrome (banner) + children pass-through (asserted by src/pages/Layout.test.jsx)
- ROUTE-04 satisfied: unknown route `/bogus` does not throw, Layout banner remains, no page section id is in the DOM (asserted by src/App.test.jsx)
- Phase 1's invariant holds: `npm test -- --run` is green
</success_criteria>

<output>
Create `.planning/phases/02-routing-page-smoke-tests/02-01-SUMMARY.md` when done. Use the standard summary template; explicitly list the section-id contract (D-09) as a "Patterns Established" entry so Plan 02-02 inherits it cleanly.
</output>
