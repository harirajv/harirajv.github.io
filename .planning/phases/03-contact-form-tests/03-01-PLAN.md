---
phase: 03-contact-form-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Contact.jsx
  - src/pages/Contact.test.jsx
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
  - FORM-04
  - FORM-05
tags:
  - testing
  - react
  - vitest
  - emailjs
  - user-event
  - contact-form

must_haves:
  truths:
    - "User can type into name/email/subject/message fields and the rendered input value reflects the typed text (FORM-01)"
    - "Submitting the contact form invokes emailjs.sendForm exactly once with the actual <form> DOM element as the 3rd argument, carrying the typed field values (FORM-02)"
    - "No real network/email is sent during tests — @emailjs/browser is mocked at module scope (FORM-03)"
    - "On a resolved sendForm, the success message 'Your message has been sent. Thank you!' is rendered AND all 4 inputs are cleared (FORM-04)"
    - "On a rejected sendForm, the generic error message 'Failed to send. Please try again.' is rendered (FORM-05)"
    - "Phase 2 PAGE-05 smoke (existing describe('Contact', ...)) still passes verbatim — no regression"
    - "npm test exits 0 on a clean checkout after npm install"
  artifacts:
    - path: "src/pages/Contact.jsx"
      provides: "State-driven status enum, conditional success/sending/error feedback, form reset on success, disabled submit during send"
      contains: "this.state = { status: 'idle' }"
    - path: "src/pages/Contact.test.jsx"
      provides: "PAGE-05 smoke (preserved) + 4 interaction tests covering FORM-01/02/04/05 (FORM-03 satisfied structurally by module-scope vi.mock)"
      contains: "describe('Contact form interactions'"
    - path: "package.json"
      provides: "@testing-library/user-event bumped to ^14.x"
      contains: "@testing-library/user-event"
    - path: "package-lock.json"
      provides: "Lockfile entries for user-event v14 and its transitive deps"
  key_links:
    - from: "src/pages/Contact.test.jsx"
      to: "@emailjs/browser"
      via: "module-scope vi.mock with default.sendForm: vi.fn()"
      pattern: "vi\\.mock\\(['\"]@emailjs/browser['\"]"
    - from: "src/pages/Contact.jsx onSubmit"
      to: "this.setState({ status })"
      via: "synchronous 'sending' before sendForm; 'success' in .then resolve; 'error' in .then reject"
      pattern: "setState.*status.*(sending|success|error)"
    - from: "src/pages/Contact.jsx render()"
      to: "feedback divs"
      via: "conditional render keyed off this.state.status"
      pattern: "this\\.state\\.status === ['\"](sending|success|error)['\"]"
    - from: "src/pages/Contact.jsx onSubmit success path"
      to: "this.form.current.reset()"
      via: "called after setState({ status: 'success' })"
      pattern: "this\\.form\\.current\\.reset\\(\\)"
---

## Objective

<objective>
Cover the contact form's interaction path end-to-end under Vitest (FORM-01..05): typing into every field, submitting, observing the mocked `emailjs.sendForm` call, and asserting state-driven success/failure feedback in the DOM. Includes a tightly scoped production refactor of `src/pages/Contact.jsx` from "static feedback divs always in DOM" to a `status`-enum-driven conditional render — without this refactor the success/error states are not assertable (the divs render unconditionally with hardcoded "Loading" / "" / "Your message has been sent" text regardless of submit outcome).

Purpose: Close the contact-form gap in the v1 testing milestone. The contact form is the portfolio's single piece of true behavior (every other page is static content); regressions on it silently drop user messages. After this plan, any regression on field-binding, submit wiring, EmailJS integration shape, or success/failure feedback fails `npm test`.

Output:
- Refactored `src/pages/Contact.jsx` with state-driven feedback (per D-01, D-02, D-03, D-04, D-05, D-06)
- Bumped `@testing-library/user-event` to `^14.x` in `package.json` + regenerated `package-lock.json` (per D-07)
- Extended `src/pages/Contact.test.jsx` with a new `describe('Contact form interactions', ...)` block of 4 tests (per D-11, D-12, D-13, D-14) — existing Phase 2 PAGE-05 smoke preserved verbatim
- `npm test` exits 0
</objective>

<execution_context>
@/Users/hariraj.venkatesan/repos/harirajv.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hariraj.venkatesan/repos/harirajv.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/03-contact-form-tests/03-CONTEXT.md
@.planning/phases/03-contact-form-tests/03-PATTERNS.md
@.planning/phases/02-routing-page-smoke-tests/02-CONTEXT.md
@.planning/phases/01-test-infrastructure/01-CONTEXT.md
@./CLAUDE.md

@src/pages/Contact.jsx
@src/pages/Contact.test.jsx
@src/pages/Home.test.jsx
@src/pages/Portfolio.test.jsx
@src/setupTests.js
@vite.config.mjs
@package.json

<interfaces>
<!-- Key contracts the executor must preserve and consume. Extracted from src/pages/Contact.jsx and the EmailJS SDK. -->
<!-- Do NOT re-derive these by exploring the codebase; they are documented here verbatim. -->

EmailJS SDK (consumed via default import from '@emailjs/browser'):
- emailjs.sendForm(serviceID: string, templateID: string, form: HTMLFormElement, options: { publicKey: string }) => Promise<{ status: number, text: string }>
- Real call site (Contact.jsx:14): emailjs.sendForm('service_eniwifa', 'contact_form', this.form.current, { publicKey: 'I-4RM4Z6x02jtxW5Z' })
- The 3rd argument is the <form> DOM node (via React.createRef()), NOT a JS object.

Contact.jsx public contract (Phase 2 + Phase 3 depend on these — DO NOT change):
- <section id="contact"> — Phase 2 PAGE-05 smoke asserts this via container.querySelector('#contact')
- <form ref={this.form}> — the form ref resolves to a real HTMLFormElement; FORM-02 reads back FormData from this node
- Input name attributes (selector contract per Phase 2 D-10 / Phase 3 D-12):
  - input[name="user_name"]
  - input[name="user_email"]
  - input[name="subject"]
  - textarea[name="message"]
- Submit button accessible name: "Send Message" (stable across all status values — D-05 / D-discretion)

Contact.jsx NEW state shape (added by this plan):
- this.state = { status: 'idle' | 'sending' | 'success' | 'error' }
- Initial value: 'idle'
- Transitions: idle → sending (synchronous, before sendForm) → success (in .then resolve) OR error (in .then reject)
- Render rules:
  - status === 'sending' → <div className="loading">Loading</div>
  - status === 'success' → <div className="sent-message">Your message has been sent. Thank you!</div>
  - status === 'error' → <div className="error-message">Failed to send. Please try again.</div>
  - status === 'idle' → none of the three render
- Side effect on 'success': this.form.current.reset() clears all inputs

Vitest globals (from vite.config.mjs test.globals: true — no imports needed):
- describe, it, expect, vi, beforeEach, afterEach

@testing-library/jest-dom matchers (from src/setupTests.js — no imports needed):
- toBeInTheDocument(), toHaveValue(value), toBeDisabled()

@testing-library/user-event v14 idiom (NEW for this repo — added by this plan):
- import userEvent from '@testing-library/user-event'
- const user = userEvent.setup()  // once per test or in beforeEach
- await user.type(input, 'value')  // async — v14 contract
- await user.click(button)         // async — v14 contract

vi.mock shape for @emailjs/browser (per D-08, Home.test.jsx analog):
- vi.mock('@emailjs/browser', () => ({ default: { sendForm: vi.fn() } }))
- After import: emailjs.sendForm.mockResolvedValueOnce({ status: 200, text: 'OK' })
- After import: emailjs.sendForm.mockRejectedValueOnce({ text: 'simulated error' })
</interfaces>
</context>

## Tasks

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor Contact.jsx to state-driven status enum with conditional feedback render</name>

  <read_first>
    - src/pages/Contact.jsx (REQUIRED — read in full before editing; this is an in-place refactor)
    - .planning/phases/03-contact-form-tests/03-CONTEXT.md §decisions (D-01 through D-06; D-09 credential preservation)
    - .planning/phases/03-contact-form-tests/03-PATTERNS.md §"src/pages/Contact.jsx (component, event-driven submit)" (exact before/after snippets for constructor, onSubmit, feedback divs, submit button)
    - src/pages/Contact.test.jsx (REQUIRED — confirm Phase 2 PAGE-05 smoke's section-id + name-attr selectors before editing Contact.jsx; the refactor must not break these)
  </read_first>

  <files>src/pages/Contact.jsx</files>

  <action>
    In-place edit of `src/pages/Contact.jsx` — preserve class-component shape, ref-based form access, EmailJS call signature, section-id, and all input `name` attributes. Only the following surfaces change:

    1. **Constructor (lines 6-9, after `this.form = React.createRef();`):** add `this.state = { status: 'idle' };`. Per D-02, `status` is a string-literal-union enum with values `'idle' | 'sending' | 'success' | 'error'` — mutually exclusive. Do not introduce booleans (`isSending`, `isSuccess`, `isError`); the enum is the single source of truth.

    2. **onSubmit handler (lines 11-19):** Restructure as follows.
       - First line after `e.preventDefault()`: call `this.setState({ status: 'sending' });` (synchronous flip per D-01; the `'sending'` UI state must be reachable before the `sendForm` promise resolves).
       - Keep the `emailjs.sendForm('service_eniwifa', 'contact_form', this.form.current, { publicKey: 'I-4RM4Z6x02jtxW5Z' })` call verbatim — credentials are config, not behavior (D-09; per-existing security posture, see threat_model).
       - Replace the resolve callback `() => { console.log('Success') }` with: set `status` to `'success'` via `this.setState` AND call `this.form.current.reset()` so the inputs clear (D-04). Note: read-then-call ordering is fine (`this.setState({ status: 'success' }); this.form.current.reset();` is the established shape from PATTERNS.md).
       - Replace the reject callback `(err) => { console.error('Failed...', err.text) }` with: set `status` to `'error'` via `this.setState`. Do NOT log `err.text` to the console; do NOT render `err.text` to the user (D-06 — avoid leaking provider internals + threat_model T-03-01). The `err` parameter is therefore unused — name it `_err` (or omit per arrow-fn rest patterns) to make this explicit.

    3. **Feedback divs (lines 66-70):** Replace the three always-rendered divs with conditional render keyed off `this.state.status` (per D-03). Render rules:
       - `this.state.status === 'sending'` → `<div className="loading">Loading</div>`
       - `this.state.status === 'error'` → `<div className="error-message">Failed to send. Please try again.</div>` (NEW generic copy; div is empty today)
       - `this.state.status === 'success'` → `<div className="sent-message">Your message has been sent. Thank you!</div>` (keep existing success copy verbatim)
       - When status is `'idle'`, none of the three render — the wrapping `<div className="my-3">` should remain so layout/spacing stays intact, but its three children only render when their status matches. Use the `{condition && <div .../>}` short-circuit idiom (matches PATTERNS.md refactor target).

    4. **Submit button (line 71):** Add `disabled={this.state.status === 'sending'}` to the `<button>` (D-05). Button text remains `"Send Message"` across all states (D-discretion; keeps `getByRole('button', { name: /send message/i })` stable). Do not change to "Sending..." text on disable — the `loading` div carries the in-flight signal.

    5. **Do NOT change:**
       - The `<section id="contact" className="contact">` opening (line 24) — Phase 2 PAGE-05 smoke depends on `#contact`.
       - The form opening `<form className="php-email-form" ref={this.form} onSubmit={this.onSubmit}>` (line 51) — the `ref={this.form}` contract is required for FORM-02's 3rd-arg-is-HTMLFormElement assertion.
       - Input `name` attributes `user_name`, `user_email`, `subject`, `message` (lines 54, 57, 61, 64) — Phase 2 D-10 selector contract, used by both PAGE-05 smoke and the new FORM-01..05 tests.
       - The `<button type="submit">Send Message</button>` text — accessible-name selector contract.
       - Imports (React, react-bootstrap-icons, @emailjs/browser) — no new imports needed.

    6. **No console.log / console.error in production paths:** Remove the existing `console.log('Success')` and `console.error('Failed...', err.text)` per threat_model T-03-01. The state transitions replace them.

    All edits are in `src/pages/Contact.jsx`. No other files touched in this task.
  </action>

  <verify>
    <automated>grep -q "this.state = { status: 'idle' }" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.jsx &amp;&amp; grep -q "this.setState({ status: 'sending' })" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.jsx &amp;&amp; grep -q "this.setState({ status: 'success' })" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.jsx &amp;&amp; grep -q "this.setState({ status: 'error' })" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.jsx &amp;&amp; grep -q "this.form.current.reset()" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.jsx &amp;&amp; grep -q "Failed to send. Please try again." /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.jsx &amp;&amp; grep -q "disabled={this.state.status === 'sending'}" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.jsx &amp;&amp; ! grep -q "console.log('Success')" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.jsx &amp;&amp; ! grep -q "console.error" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.jsx &amp;&amp; cd /Users/hariraj.venkatesan/repos/harirajv.github.io &amp;&amp; npm test -- --run src/pages/Contact.test.jsx</automated>
  </verify>

  <acceptance_criteria>
    - `Contact.jsx` contains the literal string `this.state = { status: 'idle' }` in the constructor.
    - `Contact.jsx` contains all three setState transitions: `this.setState({ status: 'sending' })`, `this.setState({ status: 'success' })`, `this.setState({ status: 'error' })`.
    - `Contact.jsx` contains `this.form.current.reset()` on the success path.
    - `Contact.jsx` contains the literal error string `Failed to send. Please try again.`.
    - `Contact.jsx` contains the literal success string `Your message has been sent. Thank you!` (preserved).
    - `Contact.jsx` contains `disabled={this.state.status === 'sending'}` on the submit `<button>`.
    - `Contact.jsx` does NOT contain `console.log('Success')` and does NOT contain `console.error` anywhere (the production logging is removed; `err.text` does not leak to the console).
    - `Contact.jsx` still contains `<section id="contact"` (Phase 2 PAGE-05 contract).
    - `Contact.jsx` still contains `ref={this.form}` on the `<form>` element.
    - `Contact.jsx` still contains all four `name="..."` attributes: `user_name`, `user_email`, `subject`, `message`.
    - `Contact.jsx` still contains the exact substring `emailjs.sendForm('service_eniwifa', 'contact_form', this.form.current, { publicKey: 'I-4RM4Z6x02jtxW5Z'})` (verbatim — credentials preserved per D-09).
    - The submit button text is exactly `Send Message` (single occurrence, not branched on status).
    - Phase 2 PAGE-05 smoke `npm test -- --run src/pages/Contact.test.jsx` passes (the existing `describe('Contact', ...)` block still goes green; the refactor must not break the pre-existing test).
  </acceptance_criteria>

  <done>
    `src/pages/Contact.jsx` is state-driven: constructor initializes `status: 'idle'`; onSubmit transitions `idle → sending → (success | error)` with `form.current.reset()` on success; the three feedback divs render conditionally on `status`; the submit button is disabled while sending; production console logging is removed; all Phase 2 selector contracts (section id, form ref, input name attrs, button name) are preserved; the existing Phase 2 PAGE-05 smoke test still passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bump @testing-library/user-event to ^14.x in package.json and regenerate lockfile</name>

  <read_first>
    - package.json (REQUIRED — confirm current pin `^13.5.0` on line 11 and its position in `dependencies` block)
    - .planning/phases/03-contact-form-tests/03-CONTEXT.md §decisions D-07 (pin style: `^`, position: `dependencies`)
    - .planning/phases/03-contact-form-tests/03-PATTERNS.md §"package.json (config, dependency bump)" (exact before/after pin)
  </read_first>

  <files>package.json, package-lock.json</files>

  <action>
    1. Edit `package.json`: change the line `"@testing-library/user-event": "^13.5.0",` (currently line 11 in the `dependencies` block) to `"@testing-library/user-event": "^14.5.2",`. Use the latest published v14 release in the `^14.x` line — `^14.5.2` is the standard pick at time of planning; if `npm install` complains about a newer patch, take the latest v14 release that resolves cleanly. Keep the entry in `dependencies` (NOT moved to `devDependencies`) to match existing style.

       Pin guard: the new value must match the regex `^\^14\.[0-9]+\.[0-9]+$` (caret, major 14, minor + patch digits). No `~`, no exact pin, no `>=`, no pre-release tags.

       No other lines in `package.json` change. Specifically, do NOT touch:
       - `@testing-library/jest-dom: ^5.17.0` (locked — Phase 1 D-establishes the matcher import path; jest-dom 5.x works with v14 user-event)
       - `@testing-library/react: ^13.4.0` (locked — Phase 1 / Phase 2 baseline)
       - The `test` / `test:watch` scripts (Phase 1 D-locked)
       - `devDependencies` (vitest, jsdom, gh-pages — Phase 1 D-locked)

    2. Run `npm install` from the project root (`/Users/hariraj.venkatesan/repos/harirajv.github.io`) to regenerate `package-lock.json` and refresh `node_modules`. `npm install` (not `npm ci`) is required because the lockfile is being mutated.

    3. After install, verify the resolved version printed by `npm ls @testing-library/user-event` starts with `14.` — if npm resolves anything else (e.g., a peerDeps conflict pins it to 13.x), surface the error and stop; do not silently proceed on a wrong-major install.

    4. **Do NOT** import `userEvent` in any test file yet — that import lands in Task 3 along with the new describe block. This task is config-only.

    Threat-model alignment: `@testing-library/user-event` is a `[KNOWN]` package per the package legitimacy gate (already in `dependencies` for the duration of this repo's history; v14.x is the current stable major from the testing-library org with millions of weekly downloads, published from `https://www.npmjs.com/package/@testing-library/user-event`). No human-verify checkpoint needed.
  </action>

  <verify>
    <automated>cd /Users/hariraj.venkatesan/repos/harirajv.github.io &amp;&amp; node -e "const v = require('./package.json').dependencies['@testing-library/user-event']; if (!/^\^14\.[0-9]+\.[0-9]+$/.test(v)) { console.error('FAIL: user-event pin is ' + v + ' (expected ^14.x.x)'); process.exit(1); } console.log('OK: ' + v);" &amp;&amp; npm ls @testing-library/user-event 2>&amp;1 | grep -E "user-event@14\." &amp;&amp; test -f package-lock.json</automated>
  </verify>

  <acceptance_criteria>
    - `package.json` `dependencies["@testing-library/user-event"]` matches the regex `^\^14\.[0-9]+\.[0-9]+$`.
    - `package.json` still has `@testing-library/user-event` in the `dependencies` block (NOT moved to `devDependencies`).
    - `npm ls @testing-library/user-event` shows a resolved version starting with `14.` (e.g., `@testing-library/user-event@14.5.2`).
    - `package-lock.json` exists and contains the new version (lockfile is regenerated; the executor does not need to inspect lockfile internals beyond existence).
    - No other line in `package.json` has changed (diff scope = 1 line in `dependencies`).
    - `node_modules/@testing-library/user-event/package.json` exists and its `version` field starts with `14.` (proves the install actually completed).
  </acceptance_criteria>

  <done>
    `@testing-library/user-event` is pinned at `^14.x.x` in `package.json` `dependencies`, `npm install` has been run, `package-lock.json` is regenerated, and `npm ls @testing-library/user-event` resolves to a 14.x version. No tests or production code touched in this task.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend Contact.test.jsx with 'Contact form interactions' describe block covering FORM-01/02/04/05</name>

  <read_first>
    - src/pages/Contact.test.jsx (REQUIRED — confirm the Phase 2 PAGE-05 smoke `describe('Contact', ...)` block on lines 1-14; the new describe is APPENDED below it and shares the `render, screen` imports plus the `Contact` import)
    - src/pages/Contact.jsx (REQUIRED — confirm the post-Task-1 state shape and feedback render rules; the tests assert on `Your message has been sent` and `Failed to send. Please try again.` literals)
    - src/pages/Home.test.jsx (canonical `vi.mock` at module scope — line 4: `vi.mock('typed.js', () => ({ default: ... }))`; mirror placement and `default:` key shape)
    - src/pages/Portfolio.test.jsx (canonical async test + `beforeEach`/`afterEach(() => vi.restoreAllMocks())` mock lifecycle on lines 5-14)
    - .planning/phases/03-contact-form-tests/03-CONTEXT.md §decisions D-08 through D-14, §specifics (the worked test example on lines 145-167)
    - .planning/phases/03-contact-form-tests/03-PATTERNS.md §"Composite test shape" (lines 199-252 — the executor mirrors this verbatim with selector substitutions)
  </read_first>

  <files>src/pages/Contact.test.jsx</files>

  <action>
    Open `src/pages/Contact.test.jsx` and APPEND a second `describe` block below the existing `describe('Contact', ...)` block. The existing PAGE-05 smoke (lines 1-14) must remain BYTE-IDENTICAL — do not reformat, do not re-order assertions, do not change selectors. The file's structure after this task:

    ```
    [existing imports — keep, extend]
    [NEW: import userEvent from '@testing-library/user-event']
    [NEW: vi.mock('@emailjs/browser', ...)]
    [NEW: import emailjs from '@emailjs/browser']

    describe('Contact', () => { ... })   // EXISTING — untouched
    describe('Contact form interactions', () => { ... })   // NEW — added by this task
    ```

    Concretely:

    1. **Top-of-file imports/mocks (added above the existing `describe('Contact', ...)`):**
       - Append `import userEvent from '@testing-library/user-event';` to the existing import block.
       - Add a module-scope `vi.mock('@emailjs/browser', () => ({ default: { sendForm: vi.fn() } }));` BELOW the imports and ABOVE the first `describe`. Vitest hoists `vi.mock` automatically, but the explicit top-of-file placement is the project idiom (Home.test.jsx).
       - Add `import emailjs from '@emailjs/browser';` AFTER the `vi.mock` line (the imported binding is the mocked module — `emailjs.sendForm` is a `vi.fn()`).
       - Do NOT import `vi`, `describe`, `it`, `expect`, `beforeEach`, `afterEach` — they are Vitest globals via `test.globals: true` in `vite.config.mjs`.

    2. **New describe block (appended below the existing Phase 2 smoke describe), with the following 4 tests:**

       - `beforeEach(() => { vi.restoreAllMocks(); })` at the top of the new describe — clears any leftover `mockResolvedValueOnce` / `mockRejectedValueOnce` between tests (mirrors Portfolio.test.jsx `afterEach` pattern, placed in `beforeEach` per D-08).

       - **Test FORM-01: `it('FORM-01: reflects typed values in all four fields', async () => { ... })`.** Selector pattern (per D-12 / Phase 2 D-10): use `container.querySelector('input[name="user_name"]')`, `container.querySelector('input[name="user_email"]')`, `container.querySelector('input[name="subject"]')`, `container.querySelector('textarea[name="message"]')`. Steps: `const user = userEvent.setup(); const { container } = render(<Contact />);` → for each of the 4 inputs, `await user.type(input, '<value>')` with a distinct test value (e.g., `'Hari'`, `'hari@example.com'`, `'Hello'`, `'Test message body'`) → assert `expect(input).toHaveValue('<value>')`. No submit in this test — typing-only.

       - **Test FORM-02: `it('FORM-02: calls emailjs.sendForm once with the form element and typed values', async () => { ... })`.** Setup: `emailjs.sendForm.mockResolvedValueOnce({ status: 200, text: 'OK' });` (mock the success path — FORM-02 only cares about the call, not the post-submit UI; success-mocked is safe because it prevents an unhandled rejection). Steps: setup user, render, type into all 4 fields with stable values (e.g., `'Hari'`, `'hari@example.com'`, `'Hello'`, `'Test message body'`), click submit. Assertions (per D-09, structurally satisfies FORM-03 via the mock itself):
         - `expect(emailjs.sendForm).toHaveBeenCalledTimes(1);`
         - `expect(emailjs.sendForm).toHaveBeenCalledWith(expect.any(String), expect.any(String), expect.any(HTMLFormElement), expect.any(Object));` (do NOT assert literal `'service_eniwifa'`, `'contact_form'`, or `publicKey: 'I-4RM4Z6x02jtxW5Z'` — per D-09, those are config and would leak credentials into test fixtures).
         - `const formArg = emailjs.sendForm.mock.calls[0][2]; const fd = new FormData(formArg); expect(fd.get('user_name')).toBe('Hari'); expect(fd.get('user_email')).toBe('hari@example.com'); expect(fd.get('subject')).toBe('Hello'); expect(fd.get('message')).toBe('Test message body');` — proves the form values reached the call.
         - To suppress the post-resolve setState warning, wrap the submit assertions in an `await screen.findByText(/has been sent/i)` (the success message — guarantees the resolved-promise tick has flushed before the test exits). This is a non-asserting use of `findByText`; the actual success/empty-fields assertion lives in FORM-04.

       - **Test FORM-04: `it('FORM-04: shows success message and clears inputs after a resolved send', async () => { ... })`.** Setup: `emailjs.sendForm.mockResolvedValueOnce({ status: 200, text: 'OK' });`. Steps: setup user, render, capture the 4 input references from `container.querySelector(...)`, type valid values into each, click submit. Assertions:
         - `expect(await screen.findByText(/has been sent/i)).toBeInTheDocument();` — auto-waitFor for the success div to appear (post-`setState({ status: 'success' })`).
         - `expect(nameInput).toHaveValue('');` AND the same for `emailInput`, `subjectInput`, `messageInput` — all four cleared by `form.current.reset()` (D-04).

       - **Test FORM-05: `it('FORM-05: shows error message after a rejected send', async () => { ... })`.** Setup: `emailjs.sendForm.mockRejectedValueOnce({ text: 'simulated error' });`. Steps: setup user, render, type valid values into all 4 required fields (the form's HTML5 `required` attributes would block submit otherwise — fill all of them), click submit. Assertion:
         - `expect(await screen.findByText(/failed to send/i)).toBeInTheDocument();` — case-insensitive substring of the generic error copy `Failed to send. Please try again.` per D-06. Do NOT assert on `err.text` or `'simulated error'` (the rejection payload is not rendered, per D-06).
         - Optionally also assert `expect(screen.queryByText(/has been sent/i)).not.toBeInTheDocument();` to prove mutual exclusion of success/error states (the enum guarantees this; Claude's discretion).

    3. **Selector and timing notes:**
       - Use `container.querySelector('input[name="..."]')` / `container.querySelector('textarea[name="..."]')` for inputs — matches Phase 2 D-10. (Placeholder-based selectors mentioned in CONTEXT specifics are an acceptable alternative; prefer `name`-attr for consistency with Phase 2.)
       - Use `screen.getByRole('button', { name: /send message/i })` for the submit button — matches the accessible-name pattern (case-insensitive regex tolerates the button text being unchanged across status transitions per D-discretion).
       - All `user.type` / `user.click` calls MUST be awaited — v14 contract.
       - Prefer `await screen.findByText(...)` over `await waitFor(() => expect(screen.queryByText(...)).toBeInTheDocument())` (D-14). `findByText` auto-`waitFor`s.

    4. **Do NOT:**
       - Use `fireEvent` — only fall back to it if `user-event` cannot express the interaction (none of these tests need that).
       - Add a `vi.mock` inside a `describe` or `beforeEach` — module-scope only.
       - Import `vi` or any Vitest global (they are ambient).
       - Import `'@testing-library/jest-dom'` in the test file (registered globally via `src/setupTests.js`).
       - Touch the existing `describe('Contact', ...)` block — that is the Phase 2 PAGE-05 smoke and must remain byte-identical.
       - Assert on the literal credentials (`service_eniwifa`, `contact_form`, `I-4RM4Z6x02jtxW5Z`) — D-09 + threat_model T-03-02 (no credential leak into test fixtures).
       - Assert on the rejection payload's `err.text` value — D-06 + threat_model T-03-01 (no provider-internals leak).

    All edits are in `src/pages/Contact.test.jsx`. No other files touched in this task.
  </action>

  <verify>
    <automated>grep -q "import userEvent from '@testing-library/user-event'" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "vi.mock('@emailjs/browser'" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "import emailjs from '@emailjs/browser'" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "describe('Contact form interactions'" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "describe('Contact'," /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "FORM-01" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "FORM-02" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "FORM-04" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "FORM-05" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "mockResolvedValueOnce" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "mockRejectedValueOnce" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "HTMLFormElement" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "new FormData" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; grep -q "findByText" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; ! grep -q "service_eniwifa" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; ! grep -q "I-4RM4Z6x02jtxW5Z" /Users/hariraj.venkatesan/repos/harirajv.github.io/src/pages/Contact.test.jsx &amp;&amp; cd /Users/hariraj.venkatesan/repos/harirajv.github.io &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `Contact.test.jsx` contains BOTH `describe('Contact',` (existing PAGE-05 smoke, untouched) AND `describe('Contact form interactions'` (new block).
    - `Contact.test.jsx` contains `import userEvent from '@testing-library/user-event'`.
    - `Contact.test.jsx` contains `vi.mock('@emailjs/browser'` at module scope (not inside any `describe`).
    - `Contact.test.jsx` contains `import emailjs from '@emailjs/browser'`.
    - `Contact.test.jsx` contains test names tagged `FORM-01`, `FORM-02`, `FORM-04`, `FORM-05` (one `it(...)` per requirement; FORM-03 is satisfied structurally by the module-scope `vi.mock` and need not have its own `it`).
    - `Contact.test.jsx` contains at least one `mockResolvedValueOnce(` and at least one `mockRejectedValueOnce(` call.
    - `Contact.test.jsx` contains `HTMLFormElement` (used in `expect.any(HTMLFormElement)` for the 3rd-arg assertion).
    - `Contact.test.jsx` contains `new FormData(` (used to read back the typed values from the captured form element).
    - `Contact.test.jsx` contains `findByText` (preferred async assertion idiom per D-14).
    - `Contact.test.jsx` does NOT contain the credential literal `service_eniwifa` and does NOT contain `I-4RM4Z6x02jtxW5Z` (per D-09 + threat_model T-03-02 — credentials stay out of test fixtures).
    - `Contact.test.jsx` does NOT contain `fireEvent` (per D-14, the v14 user-event API covers all interactions in this plan).
    - The existing Phase 2 PAGE-05 smoke (lines 4-14 in pre-edit state — the `describe('Contact', () => { it('renders the contact form with all required fields...') })` block) is preserved verbatim — same assertions, same selectors.
    - `npm test` exits 0 — the FULL suite passes, including: Phase 1 setup, Phase 2 routing/Layout/page smokes, the preserved PAGE-05 smoke, and the new 4 FORM tests.
    - No test imports `'@testing-library/jest-dom'` directly (matchers come from `setupTests.js`).
  </acceptance_criteria>

  <done>
    `src/pages/Contact.test.jsx` has a second `describe('Contact form interactions', ...)` block with 4 async tests covering FORM-01, FORM-02, FORM-04, FORM-05 (FORM-03 satisfied structurally by module-scope `vi.mock('@emailjs/browser')`); the Phase 2 PAGE-05 smoke describe is preserved verbatim; tests use the v14 `userEvent.setup()` + `await user.type/click` idiom; success tests use `findByText(/has been sent/i)`, failure test uses `findByText(/failed to send/i)`; no EmailJS credentials appear in the test file; `npm test` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → Contact form → EmailJS SDK | Untrusted user input (name, email, subject, message) is collected by the form and forwarded to the EmailJS SDK as a `<form>` element. The SDK calls EmailJS's REST API with the user-supplied values plus hardcoded service/template/publicKey credentials. |
| Contact form ← EmailJS SDK rejection | Untrusted error payload (`err.text`) returned from the EmailJS SDK on a rejected `sendForm`. Pre-existing code logged this to the console; the refactor in this plan removes that log. |
| Test fixtures → version control | The test file is committed to git. Any credential literal asserted in a test becomes part of the public repo history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `Contact.jsx` onSubmit error path | mitigate | The refactor REMOVES `console.error('Failed...', err.text)` (existing code, Contact.jsx:17) and REPLACES it with `this.setState({ status: 'error' })`. The user-facing error string is the fixed literal `"Failed to send. Please try again."` — no `err.text` is rendered to the user or logged to the console. This is a net-positive change vs. the pre-existing surface (today's code leaks `err.text` to `console.error`; post-refactor, neither console nor DOM receives provider internals). Acceptance criterion in Task 1 asserts `Contact.jsx` does NOT contain `console.error`. |
| T-03-02 | Information Disclosure | `Contact.test.jsx` test fixtures | mitigate | Per D-09, the test does NOT assert on the literal service ID (`service_eniwifa`), template ID (`contact_form`), or `publicKey` (`I-4RM4Z6x02jtxW5Z`). Instead, the 3rd-arg-is-HTMLFormElement assertion uses `expect.any(String)` + `expect.any(HTMLFormElement)` + `expect.any(Object)`, and field-value verification reads back from `new FormData(formArg)`. This keeps credentials confined to the production source (where they already are — see "PRE-EXISTING" note below) rather than duplicating them into test fixtures. Acceptance criterion in Task 3 asserts `Contact.test.jsx` does NOT contain `service_eniwifa` and does NOT contain `I-4RM4Z6x02jtxW5Z`. |
| T-03-03 | Information Disclosure (PRE-EXISTING) | `Contact.jsx:14` hardcoded EmailJS credentials | accept | The EmailJS `serviceID`, `templateID`, and `publicKey` are already hardcoded in `Contact.jsx:14` and have been committed to the public `harirajv.github.io` repo for the entire history of the Contact page. This plan PRESERVES the existing call signature verbatim per D-09 — it is NOT a Phase 3 regression and not in this phase's mitigation scope. The EmailJS `publicKey` is, by design, a public client-side identifier (EmailJS architecture requires the SDK to call from the browser with this key), and the service/template IDs are not secrets by EmailJS's security model. The mitigation path (moving to a server-side proxy, rotating to env-injected publicKey) is correctly out of scope for a testing milestone (PRD: "No new product features in this milestone"). Acceptance note: Task 1's acceptance criterion explicitly requires the call signature to remain verbatim — `emailjs.sendForm('service_eniwifa', 'contact_form', this.form.current, { publicKey: 'I-4RM4Z6x02jtxW5Z'})`. |
| T-03-04 | Tampering (incidental privacy benefit) | `Contact.jsx` onSubmit success path | mitigate | The added `this.form.current.reset()` on success (D-04) clears all input values from the DOM after a successful send. This is primarily a UX feature (the form is ready for another message), with the incidental privacy benefit that the user's typed message no longer persists on the page after submission — useful if the user steps away from a shared device. No additional mitigation needed; the existing acceptance criteria capture this behavior. |
| T-03-SC | Tampering (Supply Chain) | npm install of `@testing-library/user-event@^14.x` | mitigate | `@testing-library/user-event` is a `[KNOWN]` package: published by the testing-library org (verified at `https://www.npmjs.com/package/@testing-library/user-event`), used by millions of projects, already in this repo's `dependencies` at v13.5.0 since Phase 1. The bump to `^14.x` is a major-version upgrade within the same org's package. No new transitive dependencies of concern (v14's notable change is the async API; no new postinstall scripts, no new optional dependencies). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages are added by this plan. The lockfile is regenerated via `npm install`, leaving an auditable diff. No blocking human checkpoint needed per the package legitimacy gate. |

**Summary:** No HIGH severity threats. Two pre-existing Information Disclosure risks (T-03-01, T-03-03) — one is fixed as a side-effect of the refactor (T-03-01), one is accepted and explicitly out of scope (T-03-03). One new positive control (T-03-02) prevents credentials from spreading into test fixtures. One incidental privacy improvement (T-03-04). One routine supply-chain note (T-03-SC) on the dependency bump. ASVS L1 posture: meets the bar for a static portfolio site with a single mocked external integration.
</threat_model>

<verification>
## Phase-level Checks

After all 3 tasks complete:

1. **Static checks on production code:**
   ```bash
   grep -q "status: 'idle'" src/pages/Contact.jsx
   grep -q "this.form.current.reset()" src/pages/Contact.jsx
   grep -q "Failed to send. Please try again." src/pages/Contact.jsx
   grep -q "disabled={this.state.status === 'sending'}" src/pages/Contact.jsx
   ! grep -q "console.error" src/pages/Contact.jsx
   ! grep -q "console.log('Success')" src/pages/Contact.jsx
   ```

2. **Static checks on test code:**
   ```bash
   grep -q "describe('Contact'," src/pages/Contact.test.jsx              # PAGE-05 smoke preserved
   grep -q "describe('Contact form interactions'" src/pages/Contact.test.jsx  # new block exists
   grep -q "vi.mock('@emailjs/browser'" src/pages/Contact.test.jsx
   grep -q "userEvent.setup()" src/pages/Contact.test.jsx
   ! grep -q "service_eniwifa" src/pages/Contact.test.jsx                # no credential leak
   ! grep -q "I-4RM4Z6x02jtxW5Z" src/pages/Contact.test.jsx              # no publicKey leak
   ```

3. **Dependency pin check:**
   ```bash
   node -e "const v=require('./package.json').dependencies['@testing-library/user-event']; process.exit(/^\^14\./.test(v)?0:1)"
   npm ls @testing-library/user-event | grep -E "user-event@14\."
   ```

4. **Full test suite green:**
   ```bash
   npm test
   ```
   Expected: exit code 0. Suite includes Phase 1 setup, Phase 2 routing + 5 page smoke tests + Layout test, the preserved Phase 2 PAGE-05 Contact smoke, and the 4 new Phase 3 FORM tests. No skips, no `xit`, no `it.only`.

5. **Goal-backward verification (must_haves.truths):**
   - User types into all 4 fields → values reflect (FORM-01 passes)
   - Submit → `emailjs.sendForm` called once with `HTMLFormElement` 3rd arg + FormData contains typed values (FORM-02 passes)
   - Mock present at module scope, no real network call possible (FORM-03 satisfied structurally)
   - Resolved send → success message visible + inputs cleared (FORM-04 passes)
   - Rejected send → generic error message visible (FORM-05 passes)
</verification>

<success_criteria>
1. `npm test` exits 0 on a clean checkout (after `npm install`).
2. `src/pages/Contact.test.jsx` contains 5 test cases total: 1 in `describe('Contact', ...)` (PAGE-05 smoke, preserved verbatim) + 4 in `describe('Contact form interactions', ...)` (FORM-01, FORM-02, FORM-04, FORM-05). FORM-03 is satisfied structurally by the module-scope `vi.mock('@emailjs/browser', ...)`.
3. `src/pages/Contact.jsx` is state-driven: `this.state = { status: 'idle' }` initialized in the constructor; submit handler transitions `idle → sending → (success | error)`; the three feedback divs render conditionally; submit button is disabled while sending; `form.current.reset()` clears inputs on success; no `console.log`/`console.error` in production paths.
4. `package.json` has `@testing-library/user-event` pinned at `^14.x.x` in `dependencies`; `npm ls @testing-library/user-event` resolves to a 14.x version.
5. No EmailJS credentials (`service_eniwifa`, `contact_form`, `I-4RM4Z6x02jtxW5Z`) appear in `src/pages/Contact.test.jsx`. They remain in `src/pages/Contact.jsx` verbatim (preserved per D-09; pre-existing per T-03-03).
6. The Phase 2 PAGE-05 smoke (`describe('Contact', () => it('renders the contact form...'))`) continues to pass without modification — `<section id="contact">`, input `name` attributes, and the `Send Message` button accessible name are all preserved.
7. All 5 phase requirements (FORM-01..05) are addressed; coverage is traceable via the `requirements:` frontmatter field and the test names tagged `FORM-NN:`.
</success_criteria>

<output>
Create `.planning/phases/03-contact-form-tests/03-01-SUMMARY.md` when done.
</output>
